spring:
  datasource:
    url: jdbc:h2:mem:authservice;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  secret: test-only-secret-key-for-auth-service-unit-tests-1234
  expiration-ms: 3600000
