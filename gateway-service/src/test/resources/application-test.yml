jwt:
  secret: test-only-secret-key-for-gateway-service-unit-tests-1234

spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/**
